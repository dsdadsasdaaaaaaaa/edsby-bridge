# Edsby Bridge

A Home Assistant add-on that keeps a signed-in Edsby session and forwards the
class data Edsby's web app loads to a DayFlow relay.

Install: Settings → Add-ons → Add-on Store → ⋮ → Repositories → add this
repository's URL → install **Edsby Bridge**. See `edsby_bridge/DOCS.md`.
